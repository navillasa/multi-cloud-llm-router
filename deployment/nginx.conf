events {
    worker_connections 1024;
}

http {
    upstream frontend {
        server frontend:80;
    }
    
    upstream api {
        server llm-router:8080;
    }
    


    server {
        listen 80;
        server_name ${DEMO_DOMAIN};
        
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
        
        location / {
            return 301 https://$server_name$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name ${DEMO_DOMAIN};
        
        ssl_certificate /etc/nginx/ssl/live/${DEMO_DOMAIN}/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/live/${DEMO_DOMAIN}/privkey.pem;
        
        # Frontend
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
        
        # API
        location /api/ {
            proxy_pass http://api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 120s;
        }
        
        # LLM endpoints
        location /v1/ {
            proxy_pass http://api/v1/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 120s;
        }
        
        # Monitoring removed - demo uses built-in frontend metrics
    }
}
